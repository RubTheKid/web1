import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';
import ToDo from './src/screens/ToDo/Index';

export default function App() {
  return (
    <ToDo />
  );
}